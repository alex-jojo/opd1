# 了解 g-opd 流程差异

> 我们改进了open-policy distillation，命令行是/opd1/verl/examples/g_opd/run_qwen3-4b-g-opd.sh，这个流程是啥?和普通的opd有啥区别

<details><summary>6 previous messages</summary>

> 我先顺着这个脚本往下追一遍：看它启动了哪些 trainer/worker、开了哪些 `g_opd` 配置，再对照普通 OPD 的路径差异。
>
> 脚本本身已经说明了外层流程：数据/模型准备以后进 `verl.trainer.main_ppo`，关键差异应该藏在 trainer 训练循环、GPT rollout scorer 和 actor loss 里。我继续看这几段实现，尤其是 reroll 和 reverse-KL 的接线。
>
> 我已经看到一个很重要的点：这个脚本默认实验名虽然写了 `vanilla_opd`，但同时把 GPT rollout scorer / reroll 的一整套开关挂进 trainer；默认 `GPT_ROLLOUT_SCORE_ENABLE=False` 时，它退化成单教师 OPD/G-OPD loss 路径。接下来我找一下 repo 里“普通 OPD”的对照脚本和 loss 公式。
>
> 还有一处值得核实：GPT 打分在训练循环里插入的位置，是在 reward 前还是后、是否真的改 reward。配置注释说“logging-only”，但实际 trainer 已经接了 reroll 替换和 OPD bonus mask，所以我把调用链再定位一下，避免只按注释理解。
>
> 目前可以确定三层差异：外层脚本做自动数据/模型/日志准备；中间 trainer 在 rollout 后可用 GPT 打分并重采样低分样本；内层 actor loss 用 OPD 的 teacher logprob 来覆盖普通 GRPO advantage。最后我再看 scorer 的输出字段，确认 GPT 分数是怎么变成重采样条件的。
>
> 我有足够上下文了。整理时会把“这个脚本默认到底是不是 G-OPD”单独说清楚：按当前参数 `lambda_vals=1.0`，loss 是标准单教师 OPD；“改进”的部分主要是可选 GPT 评分/reroll 与相关 bonus，只有把 `lambda_vals` 调成非 1 或开 multi-teacher/base-model 才是配置意义上的 G-OPD 变体。
</details>

代码里这个流程实际叫 `on-policy-distillation`。这条脚本的主流程是：

1. 准备环境、读 `.env`、下载/规范化数据：DAPO-Math-17k 做 train，AIME26 做 val/test。见 [run_qwen3-4b-g-opd.sh](./verl/examples/g_opd/run_qwen3-4b-g-opd.sh:38)。
2. 准备模型：student 是 `Qwen3-1.7B`，teacher/ref 是 `Qwen3-4B-Non-Thinking-RL-Math-Step500`。见 [run_qwen3-4b-g-opd.sh](./verl/examples/g_opd/run_qwen3-4b-g-opd.sh:46)。
3. 启动 `python3 -m verl.trainer.main_ppo`，用 vLLM rollout，GRPO 框架跑训练，但 actor loss 被切到 OPD 路径：`only_reverse_kl_advantages=True`。见 [run_qwen3-4b-g-opd.sh](./verl/examples/g_opd/run_qwen3-4b-g-opd.sh:305)。
4. 每 step 里：student 先生成 rollout，然后可选 GPT 评分/reroll，然后算 reward、old logprob、teacher ref logprob，最后更新 actor。调用点在 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:2299)。
5. actor 更新时，普通 GRPO advantage 会被 OPD advantage 覆盖：`advantages = -(old_log_prob - ref_log_prob)`，也就是把 student 往 teacher 在当前 student rollout 上更高概率的 token 拉。见 [dp_actor.py](./verl/verl/workers/actor/dp_actor.py:489)。

和普通 OPD 的核心区别：

- 普通 OPD：student 自己采样，teacher 只对这些 response 算 `ref_log_prob`，loss 用 reverse KL advantage，基本公式是 `A = log p_teacher - log p_student_old`。
- 这个脚本默认其实仍是“单教师标准 OPD”：因为 `lambda_vals=1.0` 且 `multi_teacher_distill=False`，见 [run_qwen3-4b-g-opd.sh](./verl/examples/g_opd/run_qwen3-4b-g-opd.sh:329)。
- 改进部分主要是接了 GPT rollout scoring/reroll 框架：打开 `GPT_ROLLOUT_SCORE_ENABLE=True` 后，会对低分 rollout 打分、生成反馈、重采样，若新答案更好就替换原 rollout；改善足够大时还会给 OPD advantage 一个最多 1.25 倍的 bonus。逻辑在 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1556) 和 [dp_actor.py](./verl/verl/workers/actor/dp_actor.py:537)。
- 如果把 `lambda_vals` 调成非 1，才进入代码注释里的 G-OPD 变体；如果再加 `base_model_path` / `base_ref_log_prob`，会做 base-model normalization；multi-teacher 脚本则按 `opd_teacher` 在 math/code teacher 间切换。

一个小提醒：这个脚本里硬编码了 `WANDB_API_KEY` 和 `OPENAI_API_KEY`，建议尽快挪到 `.env` 并轮换密钥，避免泄露。

> 看下gpt打分的gpt_revision_suggestion是啥提取规则？为什么实际上有一半的gpt_revision_suggestion都是提取失败为空的？

<details><summary>3 previous messages</summary>

> 我来顺着 `revision_suggestion` 的生成、解析、写入日志三段查一下。这里很可能不是“正则提取失败”，而是评分 schema 本身允许高分样本返回空建议，我先把证据串起来。
>
> 我看到了一个关键边界不一致：case study 选择“低分”的代码是 `score <= threshold`，默认阈值 50；但 prompt 要求 `final_score_100 >= 50` 时 `revision_suggestion=""`。所以所有刚好 50 分的样本会被收进低分表，但建议被要求为空，这很容易看起来像“提取失败”。
>
> 我再补一眼本地有没有已经 dump 出来的 case study/rollout 表，看看空建议和分数的相关性。如果文件在容器的 `/workspace/G-OPD-logs` 而不在这个 Windows 工作区，那我就只能按代码路径判断。
</details>

不是正则提取，规则很简单：

`gpt_revision_suggestion` 直接来自 GPT JSON 里的 `revision_suggestion` 字段。代码在 [gpt_rollout_scorer.py](./verl/verl/trainer/ppo/gpt_rollout_scorer.py:450)：解析 `json.loads(text)` 后取 `str(parsed.get("revision_suggestion", ""))`，最后汇总成 `result["revision_suggestions"]`。case study 里的 `gpt_revision_suggestion` 又是直接 copy 这个值，见 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:787)。

为空主要有三个原因：

1. **prompt 明确要求高分为空**：`final_score_100 < 50` 才给建议，`final_score_100 >= 50` 必须返回 `""`。见 [gpt_rollout_scorer.py](./verl/verl/trainer/ppo/gpt_rollout_scorer.py:45)。所以如果大约一半样本 GPT 分数 >= 50，那一半空是预期行为。
2. **边界不一致**：低分/重采样选择是 `score <= threshold`，默认 threshold 也是 50，见 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1156)。但建议生成规则是 `< 50` 才非空，所以**刚好 50 分**会被当作 low case/reroll 候选，却按 prompt 要求返回空建议。
3. **case study 实际写了全量样本**：虽然算了 `low_idxs`，但 `selected_idxs = list(range(len(result["scores_100"])))`，所以 xlsx 里高分样本也都进来了，自然很多 `gpt_revision_suggestion` 为空。见 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:765)。

真正的“提取失败”通常要看同一行的 `gpt_error`：如果 `gpt_error` 非空，代码会把 `revision_suggestion` 置空；如果 `gpt_error` 为空但 `score_100 >= 50`，那不是失败，是按规则为空。如果 `score_100 < 50` 且 `gpt_error` 为空但建议仍为空，那才是 schema 约束不够强，模型违反了 prompt 但仍通过了 JSON schema，因为 schema 只要求它是 string，没有要求低分时 `minLength > 0`。

> 修改一下，“gpt_revision_suggestion 直接来自 GPT JSON 里的 revision_suggestion 字段。代码在 gpt_rollout_scorer.py (line 450)：解析 json.loads(text) 后取 str(parsed.get("revision_suggestion", ""))，最后汇总成 result["revision_suggestions"]。case study 里的 gpt_revision_suggestion 又是直接 copy 这个值，见”， 那刚好50的gpt_revision_suggestion 应该为空的，然后也不要开展第二次reroll的

<details><summary>8 previous messages</summary>

> 明白，这个边界应该统一成：`50` 属于“达标”，所以建议为空是正常的，同时不能进入 reroll 队列。我会把 reroll/low-case/pass 判定从当前的 `<= / >` 调成和 prompt 一致的 `< / >=`。
>
> 我会直接改 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1031) 里的三处阈值判断：pass 变成 `>= threshold`，case study low 和 reroll low 都变成 `< threshold`。这样 50 分既是 pass，也不会触发第二次生成。
>
> 补丁没套上，应该是文件里这些判断附近已有轻微差异或空白不同。我重新取精确上下文后分块改，范围很小。
>
> 定位到了，刚才补丁 hunk 顺序和文件顺序不一致导致没落上。我按文件顺序重新打补丁。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/ray_trainer.py` (+3 -3)
>
> </details>
>
> 阈值逻辑已经改完。现在我快速复核 diff，并用一个轻量脚本实例化/调用这几个方法，确认 49.9、50、50.1 的行为符合预期。
>
> `git diff` 在这个含中文用户名的路径上报了路径切换问题，但文件内容复核已经确认三处判断都变了。再做一次 `py_compile` 检查语法即可。
>
> 语法检查通过了。我顺手再用当前工作目录跑一次 diff，不带 `-C`，避免 Windows 路径编码那点小别扭。
</details>

已改好，在 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:740) 统一了 50 分边界：

- `score_100 >= threshold_100` 现在算 pass。
- case study 的 low 标记改成 `score_100 < threshold_100`。
- reroll 候选 `_get_low_gpt_score_idxs` 也改成 `score_100 < threshold_100`，所以刚好 50 分不会再进入 reroll，也不会开展第二次生成。

`gpt_revision_suggestion` 提取逻辑没动：50 分仍按 GPT prompt 规则应该为空。验证过 `python -m py_compile` 语法通过。

> 然后第二次re-rollout的时候的前缀，现在是什么呢？

<details><summary>1 previous message</summary>

> 我查一下当前实现里的 reroll prompt 拼接位置，重点看 `_render_reroll_prompt_suffix` 和真正替换 `gen_batch` prompt 的地方。
</details>

现在所谓“第二次 re-rollout”的 prompt 前缀其实是**整段替换后的 user prompt**，格式是：

```text
[Problem]
{原题}

[Previous Solution]
{第一次 rollout 的答案}

[GPT Feedback on Previous Solution]
{rubric_scores / revision_suggestion 的 JSON}

Please solve the problem above again.
Do not discuss the feedback explicitly.
Do not mention the previous solution.
Produce a clean corrected solution only.
Be concise.
End with the final answer.
```

然后这整段会被包进 tokenizer 的 chat template：

```python
self.tokenizer.apply_chat_template(
    [{"role": "user", "content": feedback_text}],
    add_generation_prompt=True,
    tokenize=False,
    **apply_chat_template_kwargs,
)
```

所以不是在原 prompt 后面简单 append，而是把 reroll 的 `input_ids/attention_mask/position_ids` 整行替换成这个新 user prompt。代码在 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1228) 和 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1467)。

另外当前 `max_rerollout_attempts` 被硬 clamp 到最多 1 次，所以没有第三轮；“第二次”就是第一次失败/低分后的那次 reroll。

> 我觉得第二次re-rollout的时候不应该保存太长的前缀，opd是存在前缀上下文越长，teacher/student的概率越不一致，导致会有训练不稳定性的问题对吧？是会导致loss spike，还是说会导致teacher/student的概率越来越差不多且student会喜欢说废话？

<details><summary>1 previous message</summary>

> 这个判断很对，我先把现象和机制分开说：长 reroll 前缀对 OPD 的风险主要不是“保存文本多一点”本身，而是它把训练条件分布变成了一个带上一轮答案和 GPT 反馈的异常 prompt。OPD 的 loss 只打在 response token 上，但这些 token 的 teacher/student 概率全都条件化在这个长前缀上。
</details>

对，这个担心是成立的。对 OPD 来说，长 reroll 前缀最直接的风险是 **条件分布漂移**：teacher/student 都是在 `[题目 + 上一次答案 + GPT反馈 + 再做一遍指令]` 这个异常上下文下算概率，而不是正常题目上下文下算概率。

更可能首先出现的是 **loss / advantage spike 或梯度高方差**，原因是当前 OPD advantage 近似是：

```text
A_token = log p_teacher(token | long_prefix, history)
        - log p_student_old(token | long_prefix, history)
```

前缀越长、越带有 teacher/student 理解能力差异较大的信息，比如 GPT rubric、上一轮错误解法、复杂反馈，teacher 和 student 的条件分布越容易拉开。PPO ratio 有 clip，但 advantage 本身很大时仍然会放大更新，所以会看到 actor loss、grad norm、KL 类指标抖动或 spike。

“student 会喜欢说废话”也可能发生，但它更像是第二阶段副作用：如果 reroll prompt 里长期包含上一轮解答、rubric JSON、反馈、要求“不要提反馈”等元信息，模型会学到一种反馈驱动的长上下文修正风格。虽然 loss 不训练 prompt token，只训练 response token，但 response 是在这种 prompt 条件下生成的，所以 student 可能被推向更啰嗦、更解释型、更像 case-study 修订稿的输出分布。

还有一个当前代码里的具体问题：如果 reroll prompt 超过 `prompt_length`，代码直接保留尾部：

```python
combined_ids = combined_ids[-prompt_length:]
```

这可能把 `[Problem]` 前半甚至题目截掉，只留下 previous solution / GPT feedback / 指令。这个比“前缀长”更危险，因为 teacher/student 在不完整题目条件下蒸馏，OPD 信号会变脏。

我建议把 reroll 前缀改短：

```text
[Problem]
{原题}

[Hint]
{1-3句 revision_suggestion}

Please solve the problem again. End with the final answer.
```

不要放完整 previous solution，不要放 rubric JSON；`revision_suggestion` 为空就不 reroll；再加一个硬 cap，比如 128 或 256 tokens，并且截断时优先保题目和最终指令，而不是保尾部。这样能保留“二次生成被 GPT 指导”的收益，同时减少 OPD 的长上下文不稳定。

> 第二次只放suggestion是不是就行了？那么summarize是不是也要改一下，只压缩suggestion到256词以内就行

<details><summary>24 previous messages</summary>

> 对，我也倾向这样改：第二次只给 `revision_suggestion`，不再塞 previous solution 和 rubric JSON。这样 reroll 仍然有 GPT 指导，但训练条件不会被上一轮长答案污染；我现在把 prompt 构造和 summarize prompt 一起收短到 suggestion-only。
>
> 我会把实现收成三件事：reroll prompt 只放原题和 `revision_suggestion`；空 suggestion 直接跳过 reroll；summarizer 也只压缩 suggestion，目标限制改成 256。顺便我会把“50 分为空”这个边界再对齐一遍，避免前后规则打架。
>
> 我会顺手修一个隐藏行为：当前即使某条没有 suggestion，函数只是“不改 prompt”，但后面仍会对它 reroll 一次原题，这和你的目标不一致。我会让 append 函数返回实际成功写入 suggestion prompt 的行，只 reroll 这些行。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/ray_trainer.py` (+8 -29)
>
> </details>
>
> prompt 已经改成 suggestion-only 了。接下来改 summarizer 的模板/日志措辞，并把默认 `max_reroll_context_tokens` 从 1024 收到 256。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/gpt_rollout_scorer.py` (+9 -14)
>
> </details>
>
> 现在我改默认 token cap，并让 reroll 只针对成功写入 suggestion prompt 的样本生成；这一步能避免 suggestion 为空的样本偷偷用原 prompt 再采一次。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/examples/g_opd/run_qwen3-4b-g-opd.sh` (+1 -1)
>
> </details>
>
> 还有 trainer 配置默认也最好同步到 256，避免不走这个 shell 脚本时仍然是 1024。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/config/ppo_trainer.yaml` (+1 -1)
>
> </details>
>
> 接下来是 reroll 过滤：我会让 `_append_gpt_feedback_to_reroll_prompts` 返回实际改过 prompt 的 idx 列表，然后 `_maybe_reroll_low_gpt_rollouts` 只 select 这些 idx。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/ray_trainer.py` (+17 -8)
>
> </details>
>
> 过滤逻辑也接好了。现在我再对齐 50 分的 suggestion 规则，因为我刚才看到 scorer 里有一处还写着 `<= 50`，这会和你刚才的边界要求冲突。
>
> 我发现 scorer 里还有一处历史改动把规则写成了 `<=50` 给 suggestion，这会和“50 分为空、不 reroll”冲突。我会把它也改回 `<50 / >=50`，并让低于 50 但模型没给 suggestion 时才用 overall_comment 兜底。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/gpt_rollout_scorer.py` (+4 -4)
>
> </details>
>
> 我已经把边界和 prompt 规则对齐到 `<50` 才需要 suggestion，`50` 不给 suggestion 也不 reroll。现在做一次语法检查和关键片段复核，看看有没有遗留的旧上下文/previous solution 逻辑。
>
> 语法检查已经过了；我再把无用的 previous-solution 解码清掉，避免低分样本还白白 decode 一段长答案，也让代码读起来不误导。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/ray_trainer.py` (+1 -15)
>
> </details>
>
> 我还会修一个 prompt 长度截断策略：现在如果最终 chat prompt 超过 `prompt_length`，代码取尾部，可能丢题目。这里应该直接跳过该样本 reroll，至少不产生“没题目的 suggestion prompt”。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/ray_trainer.py` (+4 -1)
>
> </details>
>
> 最后复核编译和关键搜索，确认没有 previous-solution reroll prompt 的残留，且 256 cap 生效。
>
> 只剩 `_format_gpt_feedback_for_reroll` 的旧参数名 `previous_solution` 没用了，虽然不影响行为，但我清一下签名，避免以后误用。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/ray_trainer.py` (+0 -1)
>
> </details>
>
> 小清理完成，再跑最后一遍编译。
</details>

改好了。现在第二次 reroll prompt 只包含：

```text
[Problem]
{原题}

[Revision Suggestion]
{GPT revision_suggestion}

Please solve the problem above again.
Use the revision suggestion only as guidance.
Produce a clean corrected solution only.
Be concise.
End with the final answer.
```

关键变化：

- 不再放 `Previous Solution`。
- 不再放 `rubric_scores` / GPT feedback JSON。
- `revision_suggestion` 为空的样本直接跳过 reroll，不会再用原 prompt 偷偷生成第二次。
- summarizer 改成只压缩 suggestion，输出仍是 `[Revision Suggestion] ...`，限制改成 256 token/词量语义。
- `GPT_ROLLOUT_SCORE_MAX_REROLL_CONTEXT_TOKENS` 默认从 `1024` 改成 `256`，全局 config 也同步为 `256`。
- 50 分边界也对齐：`<50` 才要求 suggestion，`>=50` suggestion 为空且不 reroll。
- 如果最终 reroll prompt 超过 `prompt_length`，现在会跳过该样本，不再保留尾部导致题目丢失。

改动文件是 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1219)、[gpt_rollout_scorer.py](./verl/verl/trainer/ppo/gpt_rollout_scorer.py:162)、[ppo_trainer.yaml](./verl/verl/trainer/config/ppo_trainer.yaml:164)、[run_qwen3-4b-g-opd.sh](./verl/examples/g_opd/run_qwen3-4b-g-opd.sh:285)。

验证过两个 Python 文件都能 `py_compile` 通过。

> [Revision Suggestion]改成[hint]，revision suggestion 也是，第二次 reroll prompt 只有suggestion了，不要包含 revision suggestion这种话。也不要有Please solve the problem above again.这种话，算是给个hint重新生成了，所以重新写第二次 reroll prompt

<details><summary>6 previous messages</summary>

> 收到，我会把 reroll 面向学生模型的提示彻底改成 hint 口吻：不出现 `Revision Suggestion` 字样，也不再加“solve again”这类显式二次作答指令，只保留原题和 `[hint]`。
>
> 我会把学生模型看到的 reroll prompt 改到最干净的形态：原题 + `[hint]`，没有“revision suggestion”、没有“solve again”、没有额外行为指令。summarizer 的文案也同步改成 hint，避免它压缩时再生成旧标题。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/ray_trainer.py` (+7 -12)
>
> </details>
>
> 学生 prompt 已经变成 `[Problem] + [hint]`。现在把 summary prompt 的输出标题改成 `[hint]`，内部文案也不再说 revision suggestion。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/gpt_rollout_scorer.py` (+7 -7)
>
> </details>
>
> 再跑一次关键搜索和编译，确认学生可见 reroll prompt 里没有旧词。注意 case-study 字段名 `gpt_revision_suggestion` 我不改，它是日志列名，不会进第二次 prompt。
</details>

已改。现在第二次 reroll 的学生可见 prompt 只有：

```text
[Problem]
{原题}

[hint]
{hint 内容}
```

不再出现 `[Revision Suggestion]`、`revision suggestion`、`Please solve the problem above again`、`Use the revision suggestion...` 这些话。

同时 summarizer 也改成产出 `[hint]`，只压缩 hint 内容，不会再生成 `[Revision Suggestion]` 标题。验证过 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1219) 和 [gpt_rollout_scorer.py](./verl/verl/trainer/ppo/gpt_rollout_scorer.py:162) 都能 `py_compile` 通过。

> 没错，如果第二次分数飞跃还有个bonus的吧？咋实现的？

<details><summary>2 previous messages</summary>

> 对，有 bonus。它不是额外 reward，而是在 actor update 里把这类样本的 OPD advantage 乘一个小系数；我把判定条件和乘法公式再精确定位一下。
>
> 刚才输出漏掉了中间选 accepted/bonus 的几行，我再拿一下准确片段。
</details>

对，有 bonus，但它不是改 reward，也不是给 GPT 分数加权，而是**在 OPD actor loss 阶段放大正向 OPD advantage**。

实现分两段。

第一段在 trainer 里打 mask：第二次 reroll 只要分数比第一次高，就接受并替换 rollout；如果提升幅度达到 30% 相对提升，就标记 `opd_reroll_bonus_mask=True`：

```python
(reroll_score_100 - initial_score_100) / max(initial_score_100, 1e-6) >= 0.30
```

位置在 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1200) 和 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1614)。

第二段在 actor update 里用这个 mask 放大 advantage。OPD advantage 是：

```python
advantages = -(old_log_prob - ref_log_prob)
```

如果该样本有 bonus mask，且这个 token 的 `opd_adv > 0`，就乘：

```python
mult = clamp(1.0 + 0.1 * opd_adv / log(2), min=1.0, max=1.25)
advantages = opd_adv * mult
```

所以最多放大到 `1.25x`，只放大正 advantage，不会把负 advantage 也放大。位置在 [dp_actor.py](./verl/verl/workers/actor/dp_actor.py:537)。

直观上：第二次分数飞跃的样本，会被认为“hint 有效、这个新 response 值得更强蒸馏”，于是 teacher 比 student 更喜欢的 token 会被更用力推一把。

> 但是这个放大倍数好像不太对，搞成跟飞跃分数相关的吧，乘以一个比较，detach，soft的函数，相对分数提升百分比越多给正梯度的bonus越大，扩大范围在1~1.25内

<details><summary>10 previous messages</summary>

> 对，现在的倍数确实是跟 `opd_adv` 自己相关，容易把 teacher/student logprob 差异大的 token 再放大一遍。更稳的是把 bonus 倍数提前在 trainer 里按 GPT 分数提升算好，actor 里只拿一个 detached 标量乘正 advantage。我来改成相对提升的 soft multiplier，范围固定 `1.0~1.25`。
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py"; $lines=Get-Content -Path $path; for($i=1190; $i -le 1216 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=1530; $i -le 1540 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=1608; $i -le 1665 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"; $lines=Get-Content -Path $path; for($i=421; $i -le 427 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=533; $i -le 557 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
> - Ran `rg -n "opd_reroll_bonus_mask|opd_bonus|reroll_bonus|improved_enough" -S "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py" "C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"`
>
> </details>
>
> 我看到 trainer 端现在已经基本按“分数提升 -> bonus scale”在算了：`opd_reroll_bonus_scale` 是 1~1.25 的浮点数。不过 actor 端还在读旧的 `opd_reroll_bonus_mask`，所以 bonus scale 还没有真正接到 loss 上；我把这半截接完整。
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py"; $lines=Get-Content -Path $path; for($i=1190; $i -le 1220 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=1528; $i -le 1542 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=1608; $i -le 1688 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=1714; $i -le 1732 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
> - Ran `rg -n "opd_reroll_bonus_mask|opd_reroll_bonus_scale|bonus_scale|bonus_mask" -S "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py" "C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"`
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"; $lines=Get-Content -Path $path; for($i=412; $i -le 428 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=533; $i -le 558 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
>
> </details>
>
> trainer 端其实已经被改成了按相对分数提升算 `opd_reroll_bonus_scale`，函数也已经是 soft saturating 到 `1~1.25`；现在差的是 actor 端还在读旧的 mask。我会把 actor 接到这个 scale 上，并保留旧 mask 兼容。
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"; $lines=Get-Content -Path $path; for($i=1; $i -le 40 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=412; $i -le 428 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=533; $i -le 560 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
> - Ran `rg -n "import math|math\." -S "C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"`
> - Ran `rg -n "opd_reroll_bonus_mask|opd_reroll_bonus_scale|bonus_scale|bonus_mask" -S "C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py" "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py"`
>
> </details>
>
> 我会把旧的 `mask` 路径替换成 `scale` 路径：trainer 传 `opd_reroll_bonus_scale`，actor 对正 OPD advantage 乘这个 detached scale。没有 scale 时兼容旧 mask 为 1.25。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/workers/actor/dp_actor.py` (+21 -5)
>
> </details>
>
> actor 已经接到 scale 了。我再检查下 trainer 里 scale 函数是否用的是你要的“相对分数提升百分比越多越大”的 soft 曲线，并跑编译。
>
> <details><summary>Ran 4 commands</summary>
>
> - Ran `python -m py_compile "C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"`
> - Ran `python -m py_compile "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py"`
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"; $lines=Get-Content -Path $path; for($i=418; $i -le 428 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=536; $i -le 568 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
> - Ran `rg -n "import math|opd_reroll_bonus_mask|opd_reroll_bonus_scale|_get_reroll_opd_bonus_scale|relative_gain|np\.exp|opd_bonus_scale" -S "C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py" "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py"`
>
> </details>
</details>

改好了，现在 bonus 跟 GPT 分数飞跃相关，不再跟 `opd_adv` 自己相关。

当前公式在 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1200)：

```python
relative_gain = max((reroll_score_100 - initial_score_100) / max(initial_score_100, 1e-6), 0.0)
bonus_scale = clip(1.0 + 0.25 * (1.0 - exp(-relative_gain / 0.30)), 1.0, 1.25)
```

也就是相对提升越大，scale 越接近 `1.25`，但 soft 饱和，不会硬跳。接受 reroll 后把这个标量存到 `opd_reroll_bonus_scale`，见 [ray_trainer.py](./verl/verl/trainer/ppo/ray_trainer.py:1536)。

actor 端现在读取 `opd_reroll_bonus_scale`，在 `no_grad` 下转 tensor、clamp 到 `1~1.25`，只对正 OPD advantage 乘这个 detached scale：

```python
advantages = torch.where(
    opd_adv > 0,
    opd_adv * reroll_bonus_scale,
    opd_adv,
)
```

位置在 [dp_actor.py](./verl/verl/workers/actor/dp_actor.py:538)。旧的 `opd_reroll_bonus_mask` 兼容路径还保留，但新路径优先。

验证过 `ray_trainer.py` 和 `dp_actor.py` 都能 `py_compile` 通过。

> 现在全部重新检查一遍，不会再有打分suggestion为空，或者别的什么的奇葩的工程错误了吧？

<details><summary>17 previous messages</summary>

> 我重新从“GPT 打分输出 → suggestion/hint 生成 → 低分筛选 → reroll prompt → 二次打分/接受 → OPD bonus → actor loss”整条链路过一遍。重点找两类问题：低分但 hint 为空导致 reroll 行为怪、以及字段名/shape/阈值不一致导致静默失效。
>
> <details><summary>Ran 4 commands</summary>
>
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\gpt_rollout_scorer.py"; $lines=Get-Content -Path $path; for($i=40; $i -le 60 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=320; $i -le 462 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=557; $i -le 620 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py"; $lines=Get-Content -Path $path; for($i=1031; $i -le 1040 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=1156; $i -le 1216 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=1219; $i -le 1490 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py"; $lines=Get-Content -Path $path; for($i=1528; $i -le 1730 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
> - Ran `$path="C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"; $lines=Get-Content -Path $path; for($i=418; $i -le 428 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }; for($i=536; $i -le 575 -and $i -le $lines.Length; $i++){ "{0}:{1}" -f $i,$lines[$i-1] }`
>
> </details>
>
> 这条链路整体现在比之前稳多了，但我看到一个还可以收紧的小点：如果 hint 极长且 summarizer 失败，当前 fallback 会保留尾部 token，理论上可能把 `[hint]` 标题截掉。我会把这个兜底改成“永远保留 `[hint]` 标题，只截 hint 内容”，这样 prompt 形态不会漂。
>
> 顺手我也会把 summarizer 的输出做一次 normalize：就算模型返回时忘了 `[hint]` 标题，也会在本地包回 `[hint]`，避免二次 prompt 格式靠模型自觉。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/ray_trainer.py` (+39 -10)
>
> </details>
>
> 我发现一个还能再兜底的地方：低分样本如果 GPT 按 schema 返回了空 `revision_suggestion`，现在会用 `overall_comment` 兜底，但如果 comment 也异常为空，仍会没有 hint。这个很少见，但工程上可以直接给一个通用 hint 兜底，保证“低分且成功打分”的样本不会因为空字段悄悄跳过。
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `python -m py_compile "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py"`
> - Ran `python -m py_compile "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\gpt_rollout_scorer.py"`
> - Ran `rg -n "summary_still_too_long|tail fallback|revision_suggestion = reason|score_100 < 50|\[hint\]|Revision Suggestion|opd_reroll_bonus_scale|opd_reroll_bonus_mask|<= threshold_100|> threshold_100" -S "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py" "C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\gpt_rollout_scorer.py" "C:\Users\缪永亮\Desktop\opd1\verl\verl\workers\actor\dp_actor.py"`
>
> </details>
>
> 我又抓到一个小兜底点：低分且 GPT 成功返回时，schema 只保证 `revision_suggestion` 是字符串，不保证非空；虽然现在会用 `overall_comment` 兜底，但如果 comment 也空，hint 还是空。我会补一个通用 hint 兜底，保证 `<50` 的成功评分样本不会因为空字段跳过 reroll。
>
> <details><summary>Edited 1 file</summary>
>
> - Wrote `./verl/verl/trainer/ppo/gpt_rollout_scorer.py` (+5 -0)
>
> </details>
>
> 我会跑一个小的本地单元式检查，不依赖模型/API：直接调用阈值、hint normalize/truncate、bonus scale，验证 49.9/50.0 的行为和 scale 范围。
>
> <details><summary>Ran 1 command</summary>
>
> - Ran `@'
> import importlib.util
> from pathlib import Path
>
> ray_path = Path(r"C:\Users\缪永亮\Desktop\opd1\verl\verl\trainer\ppo\ray_trainer.py")
> spec = importlib.util.spec_from_file_location("ray_trainer_check", ray_path)
> mod = importlib.util.module_from_spec(spec)
> spec.loader.exec_module(mod)
> T = mod.RayPPOTrainer
> print("pass", T._get_gpt_rollout_pass_flags(None, [49.9, 50.0, 50.1, None], 50.0))
> print("low", T._get_low_gpt_score_idxs(None, [49.9, 50.0, 50.1, None], 50.0))
> print("scale", [round(T._get_reroll_opd_bonus_scale(None, r, i), 4) for r, i in [(55, 49), (65, 40), (90, 10), (40, 45)]])
> '@ | python -`
>
> </details>
>
> 那个小检查因为 Windows 中文用户名在 Python 绝对路径里被当前 shell 编码成了 `???`，不是代码问题。我换成相对路径方式继续做编译和轻量行为检查。
>
> <details><summary>Ran 4 commands</summary>
>
> - Ran `python -m py_compile verl\verl\trainer\ppo\ray_trainer.py`
> - Ran `python -m py_compile verl\verl\trainer\ppo\gpt_rollout_scorer.py`
> - Ran `python -m py_compile verl\verl\workers\actor\dp_actor.py`
> - Ran `@'
> import math
> import numpy as np
>
> def finite(score):
>     try:
>         v = float(score)
>     except (TypeError, ValueError):
>         return None
>     return v if np.isfinite(v) else None
>
> def pass_flags(scores, threshold):
>     out=[]
>     for score in scores:
>         v=finite(score)
>         out.append(v is not None and v >= threshold)
>     return out
>
> def low_idxs(scores, threshold):
>     out=[]
>     for i, score in enumerate(scores):
>         v=finite(score)
>         if v is None or v < threshold:
>             out.append(i)
>     return out
>
> def bonus(reroll, initial, softness=0.30):
>     rv, iv = finite(reroll), finite(initial)
>     if rv is None or iv is None:
>         return 1.0
>     relative_gain=max((rv-iv)/max(iv,1e-6),0.0)
>     return float(np.clip(1.0 + 0.25 * (1.0 - np.exp(-relative_gain/max(float(softness),1e-6))), 1.0, 1.25))
> print('pass=', pass_flags([49.9,50,50.1,None],50))
> print('low=', low_idxs([49.9,50,50.1,None],50))
> print('bonus=', [round(bonus(r,i),4) for r,i in [(55,49),(65,40),(90,10),(40,45)]])
> '@ | python -`
>
> </details>
>
> 编译和边界检查都过了。最后我再查一遍是否有旧字段、旧 prompt 或“<=50 触发 suggestion/reroll”的残留；如果只有日志列名保留，那就是预期。
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `rg -n "Previous Solution|GPT Feedback on Previous|Please solve the problem above again|Use the revision suggestion|\[Revision Suggestion\]|final_score_100 <= 50|score_100 <= 50|<= threshold_100|> threshold_100|opd_reroll_bonus_mask|opd_reroll_bonus_scale|revision_suggestion = \"\"" -S verl\verl\trainer\ppo\ray_trainer.py verl\verl\trainer\ppo\gpt_rollout_scorer.py verl\verl\workers\actor\dp_actor.py verl\examples\g_opd\run_qwen3-4b-g-opd.sh verl\verl\trainer\config\ppo_trainer.yaml`
> - Ran `rg -n "max_reroll_context_tokens|GPT_ROLLOUT_SCORE_MAX_REROLL_CONTEXT_TOKENS|\[hint\]|Hint To Compress|score_100 < 50|score_value < threshold_100|score_value >= threshold_100" -S verl\examples\g_opd\run_qwen3-4b-g-opd.sh verl\verl\trainer\config\ppo_trainer.yaml verl\verl\trainer\ppo\ray_trainer.py verl\verl\trainer\ppo\gpt_rollout_scorer.py`
>
> </details>
>
> 搜索里一个复杂正则写坏了，我拆成固定字符串搜，避免 PowerShell 转义干扰。
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `rg -n -F "Previous Solution" verl\verl\trainer\ppo\ray_trainer.py verl\verl\trainer\ppo\gpt_rollout_scorer.py; rg -n -F "GPT Feedback on Previous" verl\verl\trainer\ppo\ray_trainer.py verl\verl\trainer\ppo\gpt_rollout_scorer.py; rg -n -F "Please solve the problem above again" verl\verl\trainer\ppo\ray_trainer.py; rg -n -F "Use the revision suggestion" verl\verl\trainer\ppo\ray_trainer.py; rg -n -F "[Revision Suggestion]" verl\verl\trainer\ppo\ray_trainer.py verl\verl\trainer\ppo\gpt_rollout_scorer.py`
> - Ran `rg -n -F "opd_reroll_bonus_mask" verl\verl\trainer\ppo\ray_trainer.py verl\verl\workers\actor\dp_actor.py; rg -n -F "opd_reroll_bonus_scale" verl\verl\trainer\ppo\ray_trainer.py verl\verl\workers\actor\dp_actor.py`
> - Ran `rg -n -F "final_score_100 <= 50" verl\verl\trainer\ppo\gpt_rollout_scorer.py; rg -n -F "score_100 <= 50" verl\verl\trainer\ppo\gpt_rollout_scorer.py; rg -n -F "<= threshold_100" verl\verl\trainer\ppo\ray_trainer.py; rg -n -F "> threshold_100" verl\verl\trainer\ppo\ray_trainer.py`
>
> </details>
</details>

我重新按整条链路查过，并顺手补了两个兜底。现在结论是：没有看到会导致“低分但 suggestion/hint 为空然后行为乱掉”的工程问题。

现在规则是：

- `score_100 >= 50`：`revision_suggestion` 为空是预期，不 reroll。
- `score_100 < 50` 且 GPT 正常返回：如果 `revision_suggestion` 为空，会先用 `overall_comment` 兜底；如果这个也空，再用一个通用 hint 兜底，所以不会空。
- GPT 超时/API 错误：可能为空，但这是打分失败，不会伪造 hint；会被统计到 skip，不会悄悄拿原 prompt reroll。
- 第二次 reroll prompt 只会是 `[Problem] + [hint]`，没有 previous solution、rubric JSON、solve-again 指令。
- hint 太长时会保留 `[hint]` 头，只截 hint 内容；如果最终 prompt 超长则跳过该样本，不会截尾导致题目丢失。
- bonus 已接到 `opd_reroll_bonus_scale`，按相对分数提升 soft 映射到 `1.0~1.25`，actor 里 detached 后只乘正 OPD advantage。

我还跑了三个文件的 `py_compile`，以及边界小检查：`49.9` 会 low/reroll，`50.0` 会 pass、不 reroll；bonus 示例也在 `1~1.25` 内。剩下唯一会空的是“打分失败”的错误样本，这是预期失败路径，不是静默工程错误。

> 推导下，这个vanilla opd训练的效果对比，哪个更好/差

我推导下来，结论是：

**当前 GPT hint reroll OPD：上限更高，稳定性略差于 vanilla OPD；如果 GPT 打分/提示靠谱，大概率比 vanilla 好；如果 GPT 评分噪声大或 reroll 占比太高，可能变差。**

**Vanilla OPD**
对原始题目 `x`，student 采样 `y`，teacher 在同一个上下文下打 token logprob：

```text
A_t = log p_teacher(y_t | x, y_<t)
    - log p_student_old(y_t | x, y_<t)
```

优点是干净、稳定、on-policy。缺点是如果 student 第一次采样很烂，OPD 只是在烂轨迹上做 teacher/student token 偏好修正，不能主动换一条更好的解题轨迹。

**现在这个版本**
低分样本先拿 GPT 生成 hint，然后重新 rollout：

```text
[Problem]
x

[hint]
h
```

如果第二次 GPT 分数 `s2 > s1`，就替换原 rollout。然后 bonus scale 是：

```text
relative_gain = max((s2 - s1) / s1, 0)
scale = 1 + 0.25 * (1 - exp(-relative_gain / 0.30))
scale ∈ [1.0, 1.25]
```

actor 里只对正 OPD advantage 乘这个 scale：

```text
A_eff = A * scale,  if A > 0
A_eff = A,          if A <= 0
```

所以它相当于：**只强化 GPT 证明“这次 reroll 明显变好”的样本，而且只强化 teacher 比 student 更支持的 token。**

**为什么可能更好**
它解决 vanilla OPD 的一个核心弱点：student 早期低质量 rollout 太多，teacher 只能在坏轨迹上蒸馏。现在低分样本会获得一次 hint-guided retry，训练分布里会多一些“可修正后的好轨迹”。这通常会提高 sample efficiency，尤其对数学推理里“第一步方向错了”的样本有帮助。

**为什么可能更差**
它不是纯 vanilla/on-policy 了。被接受的 reroll 是在 `x + hint` 条件下生成和蒸馏的，而最终评测一般只有 `x`，没有 hint。所以如果 reroll 样本占比很高，模型可能学到“在提示下修正”的能力，而不是完全学到“无提示解题”的能力。另外 GPT judge 如果偏爱更长、更像解释的答案，也可能推高输出长度。

**现在比旧版本安全很多**
旧版本放 previous solution + rubric JSON + feedback，teacher/student 概率条件分布会漂得很厉害，容易 loss spike 或学到废话风格。现在只放短 `[hint]`，风险小很多。

我会预期排序大概是：

```text
GPT hint reroll OPD > vanilla OPD
```

前提是 GPT 评分质量不错、reroll 接受率适中、bonus scale 均值不要太高。

如果看到这些现象，就可能反过来变差：

```text
reroll 占比过高
bonus_scale_mean 接近 1.2+
response length 明显变长
rule acc 没涨但 GPT score 涨
actor loss / grad norm spike
no-hint validation 不涨或下降
```

所以最终要靠 ablation 定：`vanilla OPD` vs `hint reroll no bonus` vs `hint reroll + score bonus`。理论上当前版本应该比 vanilla 更有上限，但稳定性和泛化要盯 no-hint eval。
